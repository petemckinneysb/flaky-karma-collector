{
    "name": "flaky-karma-collector",
    "version": "1.0.0",
    "description": "Run karma tests in headless mode and collect failing run seeds",
    "main": "index.ts",
    "scripts": {
        "start": "ts-node index",
        "clean": "rimraf ./output",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "Pete McKinney",
    "license": "ISC",
    "devDependencies": {
        "@types/node": "^18.8.3",
        "rimraf": "^3.0.2",
        "typescript": "^4.8.4"
    }
}
